#	Feature / Change	Core Detail	Why It Matters (User’s Reason)	Immediate Action
1	Job‑level Financials tab	New tab inside each Job detail page (alongside Schedule, Files). Holds all estimate, SoV, area, item, invoice UI.	Financial data is job‑specific; avoids “double searching” for a job.	Add UI component, route to /client/:cId/job/:jId/financials.
2	Client‑first navigation	Home → Active Clients list → expand to show all Jobs for that client → select Job. Mirrors Buildertrend but adds client aggregation.	Users manage multiple jobs per client; need quick roll‑up view and easier drill‑down.	Refactor sidebar/menu, create client view, update routing.
3	Client‑level financial roll‑up	Dashboard that aggregates totals (budget, billed %, outstanding) across all jobs of a client. Optional for MVP but high‑value.	Allows rapid AR follow‑up on high‑value clients with several projects.	Extend data model to compute aggregates; UI panel on client view.
4	Universal upload handler	Accept any file type/size (PDF, DOCX, XLSX, images, etc.). Streaming upload, size > 50 MB, tolerant of malformed files, clear error feedback.	Current uploads reset with HTTP 401; breaking point for parsing pipeline.	Build/replace upload API, add validation & retry logic.
5	Estimate parsing engine	1) OCR (if needed) → 2) LLM‑lightweight extraction → 3) Structured output: Areas → Line items → Dollar amounts → Change‑order data. Keep token usage ≤ 2 000 per doc (use Mini‑Max or hybrid model).	Generates Schedule‑of‑Values (SoV) automatically; reduces 2‑3 day manual effort.	Prototype parser, benchmark token cost, integrate with upload flow.
6	Schedule‑of‑Values (SoV) view	Shows total project value, overall completion/bar (0‑100 %). Lists areas with individual progress bars.	Provides at‑a‑glance financial health; proof for client disputes.	Consume parser output, compute totals, render UI.
7	Area drill‑down	Click an area → list of all line items (e.g., floor tile, countertop). Each item displays: budget amount, % charged/billed, status (Not Started, In‑Progress, Completed, “moving already”).	Granular visibility needed for billing justification.	Implement expandable rows, status badge logic.
8	Invoice upload & auto‑mapping	Separate invoice section inside Financials tab. Upload PDFs/CSV/images. Auto‑match to line items by description/amount/custom rules; apply payment amount, update billed % instantly.	Closes loop from estimate to payment; eliminates manual allocation.	Build invoice parser, matching algorithm, update DB fields.
9	Contract & Change‑Order support	Base contract amount displayed; UI to add/change orders that adjust total budget and propagate to SoV.	Reflects real‑world scope changes; keeps totals accurate.	Extend schema (contract, change_orders), create edit UI.
10	Audit logging	Log every upload, parse run, invoice mapping with timestamp, user ID.	Provides “proof” for older projects; satisfies compliance.	Add middleware to record actions to audit table.
11	Performance & scalability	Parsing completes within a few seconds for < 50 KB docs; upload API supports concurrent streams without degradation.	Keeps UI responsive; supports multiple jobs per client.	Load‑test upload & parser, add async processing queue if needed.
12	Security & permissions	Only authorized users can view/edit financials for a given client/job.	Protects sensitive billing data.	Enforce role‑based access control on new endpoints.
13	Future automation roadmap	After core features, add: overdue‑invoice reminders, payment‑gateway integration, exportable reports (PDF/Excel).	Extends value beyond tracking to proactive cash‑flow management.	Document roadmap; schedule post‑MVP sprints.
14	Buildertrend subscription deadline	Subscription renews tomorrow/next cycle; aim to ship core features before then.	Provides immediate replacement and avoids paying for unused tool.	Prioritize tasks 1‑4 for rapid delivery; communicate timeline.