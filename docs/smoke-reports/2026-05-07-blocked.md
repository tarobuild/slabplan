# Production smoke verification — 2026-05-07 — BLOCKED

## Summary
**Result: not run.** Production is still serving the pre-Task-#385 build, so
the Files page bug Task #386 was specifically scoped around is still firing
500s. Per Step 1 of the task ("Wait for the Files/migration fix to merge —
running a smoke test before it's patched would just catch the same error over
and over"), the smoke pass was not executed.

## Evidence
- Latest production boot log: `Server listening` at unix-ms `1778181490220`
  (~2026-05-07 19:18 UTC). No `Migrations applied` / `No pending migrations`
  log line is emitted — confirming this build predates Task #385's
  `applyMigrations(pool, …)` call in `bootstrap()`.
- Recent error (~2026-05-07 20:32 UTC, ~74 min after that boot) on
  `GET /api/jobs/bb6609ef-…/folders` → 500:
  `column "duration_seconds" does not exist`
  Stack: `listFoldersForScope` → `artifacts/api-server/src/routes/folders.ts:81`.
  The same error repeated for multiple job ids, so it is reproducible, not a
  one-off.
- The fix is present in the repo:
  - Migration `lib/db/migrations/0019_files_duration_seconds.sql` adds the
    column.
  - `artifacts/api-server/src/index.ts` calls `applyMigrations` before
    `server.listen`.
  - `replit.md` documents this as Task #385 (auto-apply on deploy).
- Nothing has been re-published since that fix landed, so production is
  running stale code against a stale schema.

## What is required to unblock
Republish the api-server artifact. On the next boot, the runner should log
either `Migrations applied` (with `0019_files_duration_seconds.sql` and
`0020_verify_auto_migrate.sql` in the file list) or `No pending migrations`
if Supabase already has them. Once that boot log is visible AND a fresh
`GET /api/jobs/:id/folders` returns 200, the smoke pass below can run.

## Flows still pending (not exercised this run)
Auth & Settings, Dashboard, Jobs (+ all sub-tabs), Files, Videos, Daily Logs,
Financials, Schedule, Leads, AI agent / PAT, MCP audit. See `.local/tasks/task-386.md`
for the full checklist.

## Production-log diff window
Start timestamp captured: `1778185931145` (just before this report).
Re-pull and diff for new 5xx after the redeploy + smoke run.
